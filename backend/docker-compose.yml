services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: mathquiz
    ports:
      - "5433:5432"
    volumes:
      - mathquiz_pgdata:/var/lib/postgresql/data

volumes:
  mathquiz_pgdata:
